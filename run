fly set-pipeline \
  -t tutorial \
  -p concourse_website \
  -c ./ci/pipeline.yml \
  -y trigger=true \
  -v GH_TOKEN="$GH_TOKEN" \
  -v github_username="$github_username" \
  -v github_password="$github_password"
